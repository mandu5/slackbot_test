name: CI Build and Request Approval

on:
  push:
    branches:
      - main

jobs:
  build-and-request:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker Image
        id: docker_build
        run: |
          echo "Building Docker image..."
          docker build -t myapp:${{ github.sha }} . || exit 1
          docker tag myapp:${{ github.sha }} myapp:latest
          echo "✅ Docker image built successfully"
          echo "image_tag=${{ github.sha }}" >> $GITHUB_OUTPUT

      - name: Send Slack Approval Request (Webhook)
        if: success()
        run: |
          COMMIT_SHORT="${{ github.sha }}"
          COMMIT_SHORT="${COMMIT_SHORT:0:7}"
          curl -X POST -H 'Content-type: application/json' \
            --data "{\"text\":\"🚀 *v${COMMIT_SHORT}* 배포 준비 완료!\\n\\n✅ Docker 이미지 빌드 성공\\n\\nProd 배포를 승인하시겠습니까?\\n\\n*배포 방법:*\\n• Slack에서 \`@SoftBank_Bot 자동 배포 시작\` 메시지 전송\\n• 또는 GitHub Actions에서 수동 승인\"}" \
            ${{ secrets.SLACK_WEBHOOK_URL }} || echo "⚠️ Slack webhook notification failed (non-blocking)"

      - name: Send Slack Approval Request (Bot API)
        if: success() && env.SLACK_BOT_TOKEN != ''
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
        id: slack
        uses: slackapi/slack-github-actions@v1.24.0
        with:
          channel-id: ${{ secrets.SLACK_CHANNEL_ID || 'C12345678' }}
          payload: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "🚀 *v${{ github.sha_short }}* 배포 준비 완료!\n\n✅ Docker 이미지 빌드 성공\n\nProd 배포를 승인하시겠습니까?"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": { "type": "plain_text", "text": "✅ Yes, Deploy!" },
                      "style": "primary",
                      "action_id": "approve_deployment",
                      "value": "${{ github.sha }}"
                    },
                    {
                      "type": "button",
                      "text": { "type": "plain_text", "text": "⛔️ Cancel" },
                      "style": "danger",
                      "action_id": "reject_deployment"
                    }
                  ]
                }
              ]
            }
